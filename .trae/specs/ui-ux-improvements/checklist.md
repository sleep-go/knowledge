* [x] Temporary files (starting with `.~`) are not visible in the KB file list.

* [x] Model selector is disabled while switching models.

* [x] Model selector shows a loading state (e.g., "Switching...") while switching.

* [x] Model selector is re-enabled after the switch completes (success or error).

* [x] Batch delete mode has a smooth transition (animation) when entering/exiting.

* [x] Batch delete checkboxes look modern (not default browser checkboxes).

* [x] Batch action bar is styled appropriately (e.g., fixed at bottom or top of sidebar).

